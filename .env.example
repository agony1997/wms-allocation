# 複製本檔為 .env 並填入實際值（.env 已被 .gitignore 排除，不會進版控）。
# docker-compose 會自動讀取 .env；Spring 應用啟動前需把這些值設為環境變數（見 docs/development.md）。

# SQL Server sa 密碼。docker-compose 用它建立容器、Spring 用它連線，兩邊共用同一個值。
# 需符合複雜度：至少 8 碼，含大小寫、數字、符號其中三類，否則容器啟動失敗。
DB_PASSWORD=your-dev-db-password

# JWT 簽章密鑰。至少 32 bytes（HS256 要求），且應為密碼學亂數、非手打。
# 產生方式：openssl rand -base64 32
JWT_SECRET=your-random-32-byte-secret
